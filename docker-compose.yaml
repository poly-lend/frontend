services:
  caddy:
    image: caddy
    container_name: liquid-caddy
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - ./src:/wwwroot
    restart: unless-stopped
